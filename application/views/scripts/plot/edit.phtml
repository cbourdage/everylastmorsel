<?php
$session = Elm::getSingleton('user/session')->user;
?>
<div class="account-form account-create container_12">
	<h1 class="turquiose">Configure your plot below.</h1>
    <?php echo $this->Message()->render(); ?>
    <form action="<?php echo $this->form->getAction() ?>" method="post" id="form-validate">
        <ul class="fieldset">
			<?php foreach($this->form->getElements() as $element) : ?>
				<?php echo $element->render(); ?>
			<?php endforeach; ?>
		</ul>
		<div class="buttons-set">
			<button type="submit" class="btn btn-blue" id="create-submit"><span>Save Changes</span></button>
		</div>
    </form>
</div>
