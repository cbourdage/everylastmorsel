<?php
$session = Elm::getSingleton('user/session');
$user = $this->user;
?>
<div class="inner-content-wrapper profile">
	<?php echo $this->partial('user/_top-display.phtml', array('user' => $user)); ?>
	<?php echo $this->partial('profile/_navigation.phtml', array('plots' => $user->getPlots())); ?>
	<?php echo $this->Message()->render(); ?>

    <?php if ($user->isMe($session->getUser())) : ?>
        <a href="#edit-about" class="button"><span>Edit About</span></a>
    <?php endif; ?>

    <?php if (strlen($user->getAbout()) > 0) : ?>
        <h2>About <?= $user->getFirstname(); ?></h2>
        <div id="about" class="blocked-content about-me">
            <p><?= str_replace('<br />', '</p><p>', nl2br($this->escape($user->getAbout()))); ?></p>
        </div>
    <?php else : ?>
        <h2>Share a little about yourself!</h2>
    <?php endif; ?>
</div>